# Understanding Python Requests

lib: requests

friendly human lib


The `requests` module allows you to send HTTP requests using Python.

The HTTP request returns a [Response Object](https://www.w3schools.com/python/ref_requests_response.asp) with all the response data (content, encoding, status, etc).
