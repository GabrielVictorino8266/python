# Utilizando módulos em python
# import library -> importa tudo
# from library import item1 -> importa um item de uma lista de itens de uma biblioteca (economiza memória)

# Library math
import math

# num =int(input('Digite um número: '))
# raiz = math.sqrt(num)
# print('A raiz de {} é {}'.format(num, math.floor(raiz)))
